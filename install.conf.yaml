- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']


- create:
   - ~/.local

- shell:
  - sh init.sh

- link:
    ~/.zshrc:
    ~/.vim_runtime:
    ~/.vimrc:
    ~/.local/bin:
    ~/.local/etc:
