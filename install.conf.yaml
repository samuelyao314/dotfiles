- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']


- create:
   - ~/.local

- shell:
  - sh init.sh

- link:
    ~/.zshrc:
    ~/.p10k.zsh:
    ~/.vim_runtime:
    ~/.vimrc:
    ~/.local/bin:
